

<div class="col-md-8">
  <p>  <%= @resume.content %>  </p>
  <hr>
  <p>
    <% if @resume.attachment.present? %>
    <%= link_to("下载简历", @resume.attachment_url ) %>
    <% else %>
    No attachment
    <% end %>
  </p>
</div>
