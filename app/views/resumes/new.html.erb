<div class="col-md-8 col-md-offset-2">
  <h3 class="text-center">投递简历到-<%= @job.title %></h3>
  <hr>
  <%= simple_form_for [@job,@resume] do |f| %>
  <%= f.input :content %>
  <%= f.input :attachment %>
  <%= f.submit "提交", disable_with: " 提交中···" %>
  <% end %>
</div>
